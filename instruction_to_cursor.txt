You are an expert software architect. Please setup a project with followings:

- Frontend: Next.js (React 19)
- Backend: NestJS with Drizzle ORM
- Database: PostgreSQL

The project should have an admin panel which includes:
- Admin Login page
- Admin Dashboard page

The project should have a staff user interface which includes:
- Staff user Login page
- Staff user Dashboard page

The project should have a standard user interface which includes:
- Standard User Registration page
- Standard User Login page
- Standard User Dashboard page

The project should have the following features:
- Building management page, only admin can create & manage Building
- Building should have following attributes: Building Name, Building No (Optional), Address-1, Address-2, Post Code
- Flat/Apartment management page, only admin can create & manage Flat/Apartment
- Flat/Apartment should have following attributes: Flat/Apartment No, Building Name (this should be drop-down and data should be populated from Building management table). 
- Standard user management page, only admin can create & manage standard user
- Staff user management page, only admin can create & manage staff user
- Standard user should have following attributes: User Name, Full Name, Phone No, Email, Building Name (this should be drop-down and data should be populated from Building management table), Flat/Apartment No (this should be drop-down and data should be populated as per the Building Name selection), Address-1 (should be populated as per the Building Name selection), Address-2 (should be populated as per the Building Name selection), Post Code (should be populated as per the Building Name selection), Gas Meter No, Gas Meter Installation Date, Gas Meter Activation Date
- Staff user should have following attributes: User Name, Full Name, Phone No, Email, Address, Operation Area Name (Optional)
- Only admin can update the attributes of Standard user & staff user 
- There should one configuration page which should following fields: Gas Unit Name, Gas Unit Price


The project should have a feature called Billing Management which includes: 
- Gas Meter Bill Generation Form with following attributes: User Name, Full Name, Phone No, Email, Flat No, House No, Gas Meter No, Gas Meter Billing Date, Previous Reading, Current Reading
- This form should have an image scanner integrated which can capture and scan an image to read gas meter digits. "Current Reading" will be populated from this scanned meter digits.
- A staff user and admin user both should have access to this "Gas Meter Bill Generation Form"
- "Current Reading" field can be editable after the image scanner populate the value for "Current Reading" field
- "Gas Meter Billing Date" will be auto populated
- There should be one more field in this form named as "Usage Quantity" which is a differnce between "Current Reading" & "Previous Reading"
- If there is no "Previous Reading" value, then "Previous Reading" will 0.
- There should be one more field in this form named as "Total Bill" which is a multiplication of Usage Quantity & Gas Unit Price (from configuration page)
- Billing Management should have a list which shows the gas bill of current month for each Gas meter which includes following attributes: Full Name, Phone No, Email, Building Name, Flat/Apartment No, Gas Meter No, Gas Meter Billing Date, Usage Quantity, Total Bill



The project should have the following features:
- Building management page, only admin can create & manage Building
- Building should have following attributes: Building Name, Building No (Optional), Address-1, Address-2, Post Code
- Flat/Apartment management page, only admin can create & manage Flat/Apartment
- Flat/Apartment should have following attributes: Flat/Apartment No, Building Name (this should be drop-down and data should be populated from Building management table). 
- Standard user management page, only admin can create & manage standard user
- Staff user management page, only admin can create & manage staff user
- Standard user should have following attributes: User Name, Full Name, Phone No, Email, Building Name (this should be drop-down and data should be populated from Building management table), Flat/Apartment No (this should be drop-down and data should be populated as per the Building Name selection), Address-1 (should be populated as per the Building Name selection), Address-2 (should be populated as per the Building Name selection), Post Code (should be populated as per the Building Name selection), Gas Meter No, Gas Meter Installation Date, Gas Meter Activation Date
- Staff user should have following attributes: User Name, Full Name, Phone No, Email, Address, Operation Area Name (Optional)
- Only admin can update the attributes of Standard user & staff user 
- There should one configuration page which should following fields: Gas Unit Name, Gas Unit Price

keep the Buildings, Flat/Apartments, Standard users, Staff users & Configuration menu visible after admin login and for admin user only